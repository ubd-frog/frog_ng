import { CommentEmojiPipe } from './comment-emoji.pipe';

describe('CommentEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
