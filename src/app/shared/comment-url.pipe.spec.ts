import { CommentUrlPipe } from './comment-url.pipe';

describe('CommentUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
