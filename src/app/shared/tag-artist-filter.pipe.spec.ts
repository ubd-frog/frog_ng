import { TagArtistFilterPipe } from './tag-artist-filter.pipe';

describe('TagArtistFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TagArtistFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
